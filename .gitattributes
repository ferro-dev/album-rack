# Enforce LF line endings in the repository across all platforms.
* text=auto eol=lf

# Explicit binary types (extend as the codebase grows)
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
